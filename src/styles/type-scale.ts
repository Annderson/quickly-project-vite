import styled from 'styled-components'
import dimensions from './dimensions'

const SansFont12 = styled.div`
  font-family: sans-serif;
  font-size: ${dimensions.px.size12};
`

const SansFont14 = styled.div`
  font-family: sans-serif;
  font-size: ${dimensions.px.size14};
`

const SansFont18 = styled.div`
  font-family: sans-serif;
  font-size: ${dimensions.px.size18};
`

const SansFont20 = styled.div`
  font-family: sans-serif;
  font-size: ${dimensions.px.size20};
`

export default {
  SansFont12,
  SansFont14,
  SansFont18,
  SansFont20,
}
